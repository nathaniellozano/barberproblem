List of funcitons-
sem_init-this function will initialize a semaphore with the value set in the code
sem_wait- this function will wait for a semaphore
sem_post- this function will signal a semaphore
customer- this function will work on the process the customer exectues, it works the way the project does but structured differently
barber- thid function will work on the process the barber executes.
____
On the terminal it will execute until barber is done.
